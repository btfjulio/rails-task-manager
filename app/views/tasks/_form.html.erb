<%= form_for(task) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :details %>
  <% if task.persisted? %>
    <%= f.label :completed %>
    <%= f.check_box :details %> 
  <% end %>
  <%= f.submit %>
<% end %>